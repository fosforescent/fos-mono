# Test info

- Name: Application Smoke Tests >> should ensure test user exists and can login
- Location: /app/infra/e2e-tests/smoke.spec.ts:27:3

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).not.toBeVisible()

Locator: getByRole('heading', { name: 'Fosforescent', exact: true })
Expected: not visible
Received: visible
Call log:
  - expect.not.toBeVisible with timeout 5000ms
  - waiting for getByRole('heading', { name: 'Fosforescent', exact: true })
    9 × locator resolved to <h1 class="text-4xl font-bold text-foreground mb-2">Fosforescent</h1>
      - unexpected value "visible"

    at /app/infra/e2e-tests/smoke.spec.ts:63:90
```

# Page snapshot

```yaml
- heading "Fosforescent" [level=1]
- paragraph: Distributed collaborative workflow system
- heading "Welcome to Fosforescent" [level=3]
- paragraph: A powerful platform for managing workflows with AI and human collaboration
- heading "✨ Key Features" [level=4]
- list:
  - listitem: • Visual graph-based workflow management
  - listitem: • AI-powered task suggestions and automation
  - listitem: • Real-time collaboration
  - listitem: • Distributed architecture
- heading "Get Started" [level=3]
- paragraph: Sign in to your account or create a new one
- tablist:
  - tab "Sign In":
    - img
    - text: Sign In
  - tab "Register" [selected]:
    - img
    - text: Register
- tabpanel "Register":
  - text: Email
  - textbox "Email": user1@fosforescent.com
  - text: Password
  - textbox "Password": user123
  - text: Confirm Password
  - textbox "Confirm Password": user123
  - checkbox "I accept the Terms & Conditions and Privacy Policy" [checked]:
    - img
  - text: I accept the Terms & Conditions and Privacy Policy
  - button "Create Account":
    - img
    - text: Create Account
- button:
  - img
- region "Notifications (F8)":
  - list
```

# Test source

```ts
   1 | import { test, expect } from '@playwright/test';
   2 |
   3 | const TEST_USER = {
   4 |   email: process.env.SMOKE_TEST_EMAIL || 'user1@fosforescent.com',
   5 |   password: process.env.SMOKE_TEST_PASSWORD || 'user123',
   6 |   name: 'Test User'
   7 | };
   8 |
   9 | test.describe('Application Smoke Tests', () => {
  10 |   test('should load the application without errors', async ({ page }) => {
  11 |     await page.goto('/');
  12 |
  13 |     // Basic smoke test - app loads and shows title
  14 |     await expect(page).toHaveTitle(/fosforescent/);
  15 |
  16 |     // Check that the main app loads without errors
  17 |     await expect(page.locator('body')).toBeVisible();
  18 |
  19 |     // Should show either login form or main app interface
  20 |     const loginForm = page.locator('input[type="email"]');
  21 |     const appInterface = page.locator('nav').or(page.locator('[data-testid="main-app"]'));
  22 |
  23 |     const hasLoginOrApp = await loginForm.isVisible() || await appInterface.isVisible();
  24 |     expect(hasLoginOrApp).toBe(true);
  25 |   });
  26 |
  27 |   test('should ensure test user exists and can login', async ({ page }) => {
  28 |     await page.goto('/');
  29 |
  30 |     // Try to login with test credentials
  31 |     await page.getByRole('tab', { name: /sign in/i }).click();
  32 |     await page.waitForTimeout(500);
  33 |
  34 |     await page.fill('#login-email', TEST_USER.email);
  35 |     await page.fill('#login-password', TEST_USER.password);
  36 |
  37 |     await page.getByRole('button', { name: /sign in/i }).click();
  38 |     await page.waitForTimeout(2000);
  39 |
  40 |     // Check if login was successful (auth heading should disappear)
  41 |     const authHeadingStillVisible = await page.getByRole('heading', { name: 'Fosforescent', exact: true }).isVisible({ timeout: 1000 }).catch(() => false);
  42 |
  43 |     if (authHeadingStillVisible) {
  44 |       // Login failed, user doesn't exist - register them
  45 |       console.log('Test user does not exist, registering...');
  46 |
  47 |       await page.goto('/');
  48 |       await page.getByRole('tab', { name: /register/i }).click();
  49 |       await page.waitForTimeout(500);
  50 |
  51 |       await page.fill('#register-email', TEST_USER.email);
  52 |       await page.fill('#register-password', TEST_USER.password);
  53 |       await page.fill('#confirm-password', TEST_USER.password);
  54 |
  55 |       // Accept terms
  56 |       await page.locator('#accept-terms').check();
  57 |
  58 |       // Submit registration
  59 |       await page.getByRole('button', { name: /create account/i }).click();
  60 |       await page.waitForTimeout(3000);
  61 |
  62 |       // Verify registration successful - should be logged in now
> 63 |       await expect(page.getByRole('heading', { name: 'Fosforescent', exact: true })).not.toBeVisible();
     |                                                                                          ^ Error: Timed out 5000ms waiting for expect(locator).not.toBeVisible()
  64 |
  65 |       console.log('✅ Test user registered successfully');
  66 |     } else {
  67 |       console.log('✅ Test user login successful');
  68 |     }
  69 |   });
  70 | });
```