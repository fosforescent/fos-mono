# Test info

- Name: Application Smoke Tests >> should load the application without errors
- Location: /home/dmn/main/code/fos/fos-mono/infra/e2e-tests/smoke.spec.ts:4:3

# Error details

```
Error: page.goto: NS_ERROR_CONNECTION_REFUSED
Call log:
  - navigating to "http://localhost:5173/", waiting until "load"

    at /home/dmn/main/code/fos/fos-mono/infra/e2e-tests/smoke.spec.ts:5:16
```

# Page snapshot

```yaml
- heading "Unable to connect" [level=1]
- paragraph: Firefox can’t establish a connection to the server at localhost:5173.
- paragraph
- list:
  - listitem: The site could be temporarily unavailable or too busy. Try again in a few moments.
  - listitem: If you are unable to load any pages, check your computer’s network connection.
  - listitem: If your computer or network is protected by a firewall or proxy, make sure that Nightly is permitted to access the web.
- button "Try Again"
```

# Test source

```ts
   1 | import { test, expect } from '@playwright/test';
   2 |
   3 | test.describe('Application Smoke Tests', () => {
   4 |   test('should load the application without errors', async ({ page }) => {
>  5 |     await page.goto('/');
     |                ^ Error: page.goto: NS_ERROR_CONNECTION_REFUSED
   6 |     
   7 |     // Basic smoke test - app loads and shows title
   8 |     await expect(page).toHaveTitle(/fosforescent/);
   9 |     
  10 |     // Check that the main app loads without errors
  11 |     await expect(page.locator('body')).toBeVisible();
  12 |     
  13 |     // Should show either login form or main app interface
  14 |     const loginForm = page.locator('input[type="email"]');
  15 |     const appInterface = page.locator('nav').or(page.locator('[data-testid="main-app"]'));
  16 |     
  17 |     const hasLoginOrApp = await loginForm.isVisible() || await appInterface.isVisible();
  18 |     expect(hasLoginOrApp).toBe(true);
  19 |   });
  20 | });
```